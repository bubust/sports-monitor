name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'   # 每 10 分鐘 ping 一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s -o /dev/null -w "%{http_code}" https://sports-monitor.onrender.com/api/status
